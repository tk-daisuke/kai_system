# Core (flask, jinja2, werkzeug, pyyaml are vendored in vendor/)
pyyaml>=6.0

# Scraping - auto_table / css_selector modes
requests>=2.28
beautifulsoup4>=4.12
pandas>=2.0
openpyxl>=3.1

# Scraping - browser_csv mode (optional)
# pip install playwright && playwright install chromium
# playwright>=1.40

# Windows Excel COM (Windows only, optional)
# pywin32>=306

# Test
pytest>=7.0
